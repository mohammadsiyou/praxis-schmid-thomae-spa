---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div
  class:list={[
    "h-[2px] my-[30px] [@media(min-width:480px)]:my-[40px] [@media(min-width:991px)]:my-[60px] bg-[#eee]",
    className,
  ]}
/>
